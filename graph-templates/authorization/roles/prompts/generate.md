Use the audited engine renderer for `authorization.roles`; do not execute this document as a script. It takes no inputs. Apply `authentication.password` (which composes `authentication.jwt`) and the `database.neon-postgres.connection` scaffold first. Review the proposal, then generate the SQL migration for the appended `roles`, `user_roles` and `role_permissions` tables with the reviewed `dbGenerate` workflow from `database.migrations`. Grant the first administrator with `bootstrapInitialAdmin` from a reviewed operator step, never from a route.
