Verify that `/api/roles` is mounted once, that its router runs `authMiddleware` and `requireAssignedRole('admin')` before any handler, and that protected application routes mount `authMiddleware` before `requireAssignedRole` or `requireAssignedPermission`. Confirm that no code reads `req.user.role` or a token role claim for these decisions, that revoking admin is refused unless another active administrator remains, that code which suspends or deletes users calls `assertNotLastActiveAdmin`, that built-in roles cannot be renamed or deleted, and that the migration for the three tables was generated and reviewed. Run the generated tests and a strict TypeScript build.
