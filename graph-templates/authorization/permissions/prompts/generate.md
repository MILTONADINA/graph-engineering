Use the audited engine renderer for `authorization.permissions`; do not execute this document as a script. Supply a bounded, unique `resource:action` vocabulary and an application-owned `src/services/permissionAuthorizer.ts` exporting `hasPermission(userId, permission): Promise<boolean>`. The authorizer must read current trusted grants and deny by default. Review the generated proposal and mount `authMiddleware` before `requirePermission` on each protected route.
