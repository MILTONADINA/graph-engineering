Use the audited deterministic renderer after explicitly provisioning its pinned dependencies. Propose the declared files, exact environment-helper insertions and dbGenerate script; refuse divergent existing content. Rendering must not install dependencies, execute target scripts or contact a database. Compile only in the approved sandbox. Report proposed files and exports, not a claimed live Neon connection.
