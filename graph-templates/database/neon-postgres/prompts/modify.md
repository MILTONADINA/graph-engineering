This node's own files rarely change after generation — new tables are added by `backend.repository` (append to `schema.ts`), not by re-running this node. If you need a different Postgres provider (not Neon), that's a different template (`database.postgres`, `planned`), not a modification of this one.
