1. If `README.md` doesn't exist, create it with a `# {{input.projectName}}` heading followed by the Quick Start section. 2. If it exists and contains the marker pair, replace only the content between them. 3. If it exists without the markers, insert the section at the top after any `# Title` line and add the markers, leaving existing content below untouched. 4. Read `.graph/manifest.json` to decide whether to include the `npm run dbGenerate && npm run dbMigrate` step (only if a `database.*` node has run).
