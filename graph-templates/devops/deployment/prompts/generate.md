1. Confirm that the target application already has the exact reviewed `devops.docker` Dockerfile for the requested port. Apply and review `devops.docker` separately if it is absent; do not treat a same-proposal overlay as an installed prerequisite.
2. Supply `target: "ecs-express"`, an explicit metadata timestamp, an existing digest-pinned private ECR URI, the three distinct IAM role ARNs, explicit subnet/security-group IDs, full secret-reference ARNs or `[]`, and the unverified-prerequisites acknowledgement. Use the `dev` CI branch unless the owner explicitly selects another trusted branch.
3. Invoke the audited `devops.deployment` renderer. Review the build/test-only CI workflow, sanitized public `deployment.schema.json`, and private ECS request separately. Do not call AWS, build or push the image, commit the private descriptor, or claim deployment success.
