1. Confirm the existing Dockerfile byte-for-byte matches the reviewed `devops.docker` output for the request port. A missing or different file is a prerequisite failure, not permission to bypass the AWS descriptor verifier.
2. Validate that `deployment.schema.json` contains only the target, static file paths, metadata timestamp, redacted project name and `envInventoryComplete: false` with an intentionally empty environment inventory. It must contain no caller-derived names, ECR URI/digest, AWS account, IAM ARN, VPC ID, or secret ARN. Confirm the ECS request remains at `deploy/ecs-express-create-service.json` and is excluded from cloud export and Git publication.
3. Run the focused offline engine test. It proves renderer composition and privacy guards, not image provenance, IAM/VPC permissions, live service health, AWS cost, or a deployment. A separate operator must review those before any chargeable API action.
