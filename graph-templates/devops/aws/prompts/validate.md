1. Verify the local descriptor exists, parses as JSON and contains only the reviewed ECS Express Mode request fields. Confirm the image URI ends in `@sha256:` plus 64 hex digits; `primaryContainer.secrets` contains only full ARNs and `primaryContainer.environment` is absent.
2. Verify the target Dockerfile byte-for-byte against the reviewed `devops.docker` source for the selected port. A mismatch means this node's prerequisite failed; correct the Dockerfile through its owning node or revise the port.
3. Run the focused engine test. This checks rendering and cloud export exclusion, not live AWS. Before any separate deployment, an operator must validate image provenance, IAM trust/policies, subnet/security-group placement and routes, secret access, `/` health response and cost.
