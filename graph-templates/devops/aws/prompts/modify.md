1. Detect the existing descriptor, then re-render with reviewed inputs. If it is byte-identical, report no changes.
2. If the descriptor differs, stop with the audited renderer's existing-file conflict; obtain an explicit migration plan before changing it. Never overwrite an operator-edited request or call AWS.
