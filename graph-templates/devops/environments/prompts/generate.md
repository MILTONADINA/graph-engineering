1. Read `architecture.json`'s `data.nodes[]`. 2. For each node id, look up its `environment.variables` in `template-registry.json`. 3. Merge into one deduplicated list (a variable declared by two nodes with the same name is one row, `usedBy` lists both). 4. Write `.env.example` (name + `=` + blank/placeholder, grouped by owning node as a comment header) and `docs/ENVIRONMENT.md` (the same data as a Markdown table: name, required, secret, description, used by). Never populate a real value — only names/placeholders.
