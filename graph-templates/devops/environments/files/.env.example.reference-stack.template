# Generated by devops.environments — illustrative for the full reference-stack
# graph (project.node-express + database.neon-postgres.connection + storage.aws-s3
# + authentication.password/jwt). A real run derives this list from whichever
# nodes architecture.json actually selected, not this fixed set.

# project.node-express
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# database.neon-postgres.connection
DATABASE_URL=

# storage.aws-s3
AWS_ENDPOINT_URL_S3=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=media

# authentication.password / authentication.jwt
ACCESS_TOKEN_SECRET=
SALT_ROUNDS=12
