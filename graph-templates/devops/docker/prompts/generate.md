Use only the audited renderer: Node 24, validated nonprivileged port, matching package lock, src/app.ts and src-to-dist TypeScript configuration. Render the three installed assets without executing target npm/build scripts on the host. Keep the build-context allowlist, all secret/private-path exclusions, lifecycle suppression and nonroot runtime intact. Verify only in an explicitly approved isolated Docker build/run; never provision images or dependencies implicitly.
