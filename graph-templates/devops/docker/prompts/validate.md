Check the three declared outputs and supported input/configuration prerequisites. In an explicitly approved isolated container build, verify the exact generated Dockerfile, nonroot runtime, production-only dependencies, excluded .env*/Git/private graph files and disabled lifecycle hooks. Rendering itself executes no host build or target script. Preprovision trusted Node 24 images/dependencies before offline verification.
