Render the installed safe CI asset with validated literal Node24/dev-branch inputs. Automatic PR/push jobs must have read-only permissions, pinned action SHAs, no persisted checkout credentials and no secrets references. Append migrations.yml.fragment only when includeMigrations is explicitly true, dbMigrate exists, and migrationEnvironment is explicitly selected; preserve the manual boolean, exact trusted branch, successful-check dependency and immutable SHA checkout. An environment reference is not evidence that protected-environment reviews are configured.
