Parse YAML and confirm pinned actions, read-only permission and no secrets in automatic jobs. Missing build/test scripts are errors. If migrations are enabled, require dbMigrate, explicit dispatch boolean, exact trusted branch, successful checks, immutable event-SHA checkout and a selected environment. Confirm environment protection separately in GitHub; do not claim it from YAML. Never run target scripts or change GitHub settings during rendering.
