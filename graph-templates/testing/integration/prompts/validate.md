Validate exports `getTestDatabase`, `truncateAllTables`, `closeTestDatabase`; build strictly and run guard tests without a database connection. Verify missing/production URLs, URL routing overrides, application-target reuse, missing destructive acknowledgement and invalid identifiers fail before connection. Separately test real SQL only against an isolated disposable database: truncation must be atomic, limited to the explicitly listed public-schema tables and never use CASCADE.
