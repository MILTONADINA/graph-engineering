Write `tests/setup/testDatabase.ts` verbatim and include its guard tests. Declare `pg`, `@types/pg`, Drizzle and Vitest. Require an explicit disposable `TEST_DATABASE_URL` and `NODE_ENV=test`; never add a `DATABASE_URL` fallback. Only permit destructive cleanup with `GRAPH_TEST_DATABASE_ALLOW_TRUNCATE=1`. Close the pool at suite teardown. Build and test in the approved isolated verifier, not by executing template hooks on the host.
