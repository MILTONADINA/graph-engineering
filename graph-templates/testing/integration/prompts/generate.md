Write `tests/setup/testDatabase.ts` verbatim. Confirm `TEST_DATABASE_URL` (or a documented `DATABASE_URL` fallback) is set in the target environment before relying on this in CI. Run `npm run build`.
