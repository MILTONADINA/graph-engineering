Verify the root's public API origin, the client's exact audited source and
generated API test. Run a provisioned typecheck, unit test and build. Review
same-site cookies and exact credentialed CORS for the actual deployment.
