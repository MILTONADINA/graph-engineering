Render `frontend.react` only after the reviewed `project.vite-react` root.
The deterministic engine emits the shared audited `apiFetch` client and test;
catalog prompts are not executable hooks and do not install dependencies.
