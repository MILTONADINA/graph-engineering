1. Confirm `lib/apiClient.ts` (`frontend.nextjs`) and the backend's `/api/auth/*` routes (`authentication.password`) exist. 2. Render `files/AuthContext.tsx.template` to `lib/auth/AuthContext.tsx`. 3. Render the login/register page templates. 4. Apply both `files.modify` operations to `app/layout.tsx` at its two markers. 5. Run `npm run build`. 6. Report `files`, `exports`, `routes`.
