file-exists → exports (`beginLogin`, `completeLogin`, `linkAccount`) → `oauthRoutes` mounted at `/api/auth` → strict `npm run build` → `npm test -- authenticationOauth`. A startup error naming an OAuth variable means a client id/secret or `OAUTH_REDIRECT_BASE_URL` is missing; there are deliberately no defaults. Confirm each provider's registered redirect URI equals the generated one exactly.
