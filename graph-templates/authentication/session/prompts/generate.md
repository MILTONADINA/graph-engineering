Do not hand-copy these files. Render `authentication.session` with the engine's audited deterministic renderer, which validates the inputs, checks that `authentication.password` has already been applied (`src/repository/Authentication.ts`, `src/services/authIdentity.ts`, `src/middlewares/authMiddleware.ts`, `userTable` in `src/config/schema.ts`), and proposes the session store, session middleware, routes, schema fragment, `SESSION_SECRET` binding, `/api/session` mount, the reviewed password-reset revocation edits to `src/repository/Authentication.ts` and `src/services/authenticationService.ts`, and tests as a reviewable change. Then configure `SESSION_SECRET` (no default), choose `SESSION_STORE`, generate and review the `sessions` migration, and run `npm run build` and `npm test -- authenticationSession`.
