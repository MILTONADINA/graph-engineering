Confirm `src/sessions/session.ts` exports `requireSession`, `csrfProtection`, `startSession`, `regenerateSession`, `destroySession`, `revokeSession` and `revokeUserSessions`, that `src/app.ts` mounts `sessionRoutes` at `/api/session`, and that `SESSION_SECRET` is listed in `requiredEnvironmentVariables` with no default. Run `npm run build` and `npm test -- authenticationSession` with `SESSION_SECRET` set. A startup error about `SESSION_STORE=memory` under `NODE_ENV=production` is intended: configure `SESSION_STORE=postgres` or a custom production store.
