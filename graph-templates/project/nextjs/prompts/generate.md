1. Confirm the target directory has no `package.json` declaring `next` (idempotency detection). 2. Resolve inputs (`projectName` required; others default). 3. Render every file under `files/` with `{{input.*}}` substituted. 4. Initialize `.graph/manifest.json`. 5. Run `npm install`. 6. Run `npm run build` to confirm the scaffold compiles. 7. Report `files` and `entrypoint: app/layout.tsx`.
