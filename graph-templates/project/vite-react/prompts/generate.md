The deterministic engine renderer handles `project.vite-react`. Supply validated
inputs and review the proposal. The catalog files describe the emitted contract;
this note is not an executable hook. Rendering does not install dependencies,
run build commands, start a server or generate a lockfile.
