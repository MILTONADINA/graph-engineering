Use the audited `api.search` engine renderer, not this document as an executable script. Select one existing reviewed Drizzle `pgTable`, one to four actual text/varchar fields, and an authenticated search route. Review the proposed GIN expression index, generated SQL, application mount and test. Do not enable the route until a matching migration is generated, reviewed and applied through the separately guarded database workflow.
