Confirm that the table and columns are AST-verified literals, query/pagination values are bound parameters, the route calls `authMiddleware` before the search handler, and ranked results are ordered by rank descending then UUID ascending. Generate the pending migration offline and inspect its GIN expression; apply it only with operator review. Sensitive or tenant-scoped tables need additional row-level predicates, not just authentication.
