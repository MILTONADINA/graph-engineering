Route file exists → repository contains `FILTERABLE_FIELDS` → `npm run build` → manually re-verify the security invariant in `template.yaml`'s `no-unlisted-column-passthrough` check: grep the repository file for `options.filters[` and `options.sortBy` and confirm every occurrence is inside the two allowlist-map lookups, never a direct property/bracket access into `{{input.tableExportName}}`.
