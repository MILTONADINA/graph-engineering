Use the audited engine renderer for `api.webhooks`; this document is not an executable script. Review its mount after CORS/Helmet and before Morgan/`express.json()`, including terminal 405/404 handling under the webhook prefix. Supply a separately generated production `WEBHOOK_HMAC_SECRET` and an application-owned atomic durable `enqueueVerifiedWebhook` inbox before deployment. The fixed v1 wire format is not a generic Stripe/GitHub signature adapter.
