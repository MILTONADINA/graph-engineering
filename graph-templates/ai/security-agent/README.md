# ai.security-agent

**Lighter treatment.** A focused re-check of `ai.validation-agent`'s "security problems" category, not a new artifact-producing pipeline stage. Checks CORS config, upload/validation pairing, RBAC coverage on protected routes, literal secrets, and tenant-isolation repository patches.

**Hands off to.** `ai.code-review-agent`.
