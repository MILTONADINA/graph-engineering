1. `requirements.json` validates against `artifacts/requirements.schema.json`. 2. `data.entities` is non-empty. 3. `data.features` is non-empty and every entry has `id`, `title`, `priority`. 4. No duplicate `feature.id`. 5. If any `feature.requiresAuth` is `true`, downstream agents (`ai.architect-agent`) must select an `authentication` value other than `none` — flag this as a cross-artifact expectation the Validation Agent checks later, not something this agent verifies itself.
