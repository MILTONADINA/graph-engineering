1. `database.schema.json` validates against `artifacts/database.schema.json`. 2. Every `requirements.json` entity name has a matching `data.tables[].name`. 3. Every table has an `id` primary-key column (explicit or implied by convention — note which). 4. `tenantScoped` values agree with `architecture.json`'s multi-tenancy stack decision; mismatches are reported, not silently resolved.
