1. `architecture.json` validates against `artifacts/architecture.schema.json`. 2. Every `data.nodes[].id` exists in the template registry with `status: implemented`. 3. `data.nodes` order respects every selected node's `requires` edges (no node appears before something it requires). 4. No two selected nodes are mutually `conflicts`. 5. If `requirements.json` has any `feature.requiresAuth: true`, `stack.authentication !== 'none'`; same for `requiresFileStorage` → `stack.storage !== 'none'`. 6. `project.node-express` (or another `project.*` root) is `data.nodes[0]`.
