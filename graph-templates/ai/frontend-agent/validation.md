1. `frontend.schema.json` validates against `artifacts/frontend.schema.json`. 2. Every `api.schema.json` route with `auth: required` maps to a page with `requiresAuth: true`. 3. If any page has `kind: form`, `frontend.forms` appears in `architecture.json`'s selected nodes; same for `kind: table` → `frontend.tables`. 4. If `auth.schema.json`'s `data.strategy` is not `none`, `frontend.authentication` appears in the selected nodes, and `authentication.password` appears in the backend half of the same graph (checked via the registry's `dependsOn`, not assumed). 5. No `kind: dashboard` page has a corresponding generated node — `frontend.dashboards` is `planned`; such pages are recorded only. 6. If `architecture.json`'s `stack.frontend` is `react`, the agent's output states this is unimplemented rather than silently falling back to `project.nextjs`.
