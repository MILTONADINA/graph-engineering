# ai.documentation-agent

**Lighter treatment.** Invokes the four `documentation.*` nodes (`setup`, `architecture`, `api`, `agent-context`) once other agents have produced enough artifact detail to document. Never fabricates content a source artifact doesn't have.

**Hands off to.** `ai.validation-agent`.
