1. `api.schema.json` validates against `artifacts/api.schema.json`. 2. Every `database.schema.json` table has ≥1 route referencing it via `route.entity`. 3. For each invoked node, its reported `outputs.files` exist on disk (a mismatch means the invocation reported success without actually writing — treat as an error, not a warning). 4. `npm run build` succeeds after all invocations.
