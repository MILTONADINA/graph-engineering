# Context intelligence and storage lifecycle

All APIs below are methods on `ContextEngine`, except static `restoreBackup`. Context remains private to the project's local data directory. These methods never publish, commit, or push.

## Summaries and reusable solutions

`listSummaries(snapshotId?)` returns deterministic file → directory → repository summaries. File summaries describe AST declarations/imports with exact source references; they do not claim to explain behavior. Parent hashes depend on sorted child content hashes, so an unrelated subtree does not invalidate an unchanged summary. Listings are bounded to 80 declarations, 30 imports, and 100 displayed child entries; structured child identities remain complete. A tightened exclusion policy rebuilds filtered aggregates even when reading an old snapshot.

`putSolution({key, inputs, value, sources, snapshotId?})` stores a private reusable result, and `getSolution({key, inputs, snapshotId?})` returns a match or `null`. Matching requires the same project, snapshot, entire policy, canonical JSON inputs, key, and parser version. Source references must match indexed contents. Inputs and result text are credential-screened; results are at most 100,000 characters with 1–100 source references. This is an evidence cache, **not authorization to apply a patch or execute a command**. An explicitly requested old snapshot can intentionally retrieve its old result; omit the snapshot to validate against the current working tree. Caller-owned tool/model/config versions belong in `inputs`.

## Memory review and graph limits

`reviewMemories(snapshotId?)` records and returns review flags for changed/missing/excluded source evidence, missing provenance, invalid supersession, exact inconsistencies in explicitly reviewed typed assertions, and opposing free-text wording. Lexical flags are heuristics; structured flags compare declared subject/predicate/scope, types, exclusivity, and temporal overlap—not general semantic truth. See [explicit memory assertions](memory-assertions.md) for the separate review/accept/share workflow and limits. At most 2,000 memory records are reviewed per call; larger projects fail explicitly rather than silently omitting records. Review flags never accept, delete, or supersede a memory. Invalid imported supersession does not choose a winner. Stale or conflicting required constraints remain mandatory context until a human resolves them.

Tree-sitter parsing covers the seven supported language families. A pinned TypeScript 5.9.3 compiler additionally binds direct TS/JS calls and constructors through named/default/namespace imports, unambiguous re-exports, and immutable identifier aliases. These edges carry `resolution: {kind: "static", engine: "typescript", version: "5.9.3"}`. Relative imports, indexed project aliases, and declared in-repository workspace packages are supported within the boundaries below. An isolated in-memory compiler host sees only indexed, hash-validated source and inert JSON/JSONC configuration: it never loads configuration from disk, executes plugins, discovers `node_modules`, or fetches network source. Compiler-proven declaration bindings are **not runtime dispatch proofs**. Remaining lexical candidates stay explicitly heuristic; arbitrary object methods, dynamic imports/dispatch, overloaded or reassigned callables, ambiguous re-exports, syntax-error trees, and implicit cross-file script globals are not statically resolved. Python and Go have the separate bounded compiler-backed passes below; Rust, Java, and C# currently retain syntax/lexical coverage rather than compiler-backed cross-module resolution.

Alias resolution uses each importing file's nearest indexed `tsconfig.json`/`jsconfig.json` (also `.jsonc`), bounded relative `extends`, `baseUrl`, and exact/single-star `paths`. Exact aliases precede wildcard aliases; multiple distinct indexed targets are treated as ambiguous. Package lookup requires a unique self package or an indexed workspace declaration (`workspaces` array or `workspaces.packages`), with exact or single-segment-star workspace patterns. It supports root/subpath/single-star `exports`, leading `types` conditions, unique condition-independent string targets, and legacy `types`/`typings`/`main` when exports are absent. Exports encapsulation, explicit null targets, duplicate package names, traversal, duplicate JSON keys, stale hashes, and missing/excluded targets fail closed. Conditional branches that depend on an unknown environment are not guessed. Mapping edges include exact configuration/package references in `resolution.sources`; tightening local exclusions downgrades historical bindings, and cloud traversal drops bindings whose mapping evidence is not exportable.

Configuration limits are 1,024 indexed JSON/JSONC candidates and 1 MiB aggregate (lockfiles omitted), 64 KiB per parsed record, 16 relative inheritance levels, 128 mappings/workspace patterns, eight alias replacement candidates, and 64 provenance records per edge. Package-based or multiple-parent `extends`, `typesVersions`, `rootDirs`, `moduleSuffixes`, custom conditions, recursive/negated workspace globs, installed dependencies, and project-reference/build-configuration selection remain unsupported. The nearest-file configuration rule is not a reconstruction of every program a build tool may create; config `files`/`include`/`exclude` do not restrict the already policy-filtered snapshot. Plugins are inert data and never loaded. Unsupported resolution produces coverage diagnostics rather than synthesized targets. Intermediate re-export source files also appear in binding provenance; a private barrel cannot silently connect otherwise public caller/target nodes during cloud traversal.

### Isolated Python bindings

On macOS/Linux, an available CPython 3.8+ in a fixed trusted system/Homebrew location runs the packaged analyzer with `-I -S -B`, an empty environment, fixed arguments, and bounded snapshot JSON on stdin. It uses standard-library `ast` and `symtable`; it never imports, evaluates, or executes repository modules, startup hooks, config, dependencies, or arbitrary source paths. Actual interpreter version appears as `resolution.engine: "cpython"`/`resolution.version`; interpreter/helper identity participates in snapshot identity. Runtime discovery is cached for the service lifetime, so restart after provisioning or replacing Python. An absent/unsupported runtime (including current Windows fallback) leaves explicit syntax-only coverage rather than downloading or invoking an untrusted interpreter.

The pass resolves unique direct functions/classes, lexical nested calls, explicit absolute/relative imports, import aliases, module-qualified calls, and re-exports in regular packages rooted at the indexed repository. It rejects shadowing/reassignment, decorated or conditionally defined callable bindings, star imports, dynamic namespace evaluation, explicit module attribute mutation, ambiguous module/package names, missing package initializers, and unsupported dynamic/object dispatch. Comprehension/lambda/annotation/default-expression calls, namespace packages, custom `sys.path`/`src` layouts, installed dependencies, virtual-method dispatch, and whole-program runtime mutation analysis remain outside this pass. Source/package/re-export provenance receives the same current-policy and cloud traversal checks as TS/JS edges.

Bounds are 500 Python files, 4 MiB source, 16 MiB serialized input, 100,000 AST nodes, 5,000 output updates, 2 MiB process output, a 5-second wall deadline, and a 3-second CPU limit. The parent checks elapsed monotonic time, including synchronous spawn, before accepting output or completion; a delayed timer cannot authorize late evidence. Linux additionally enforces a 256-MiB address-space limit. macOS rejects `RLIMIT_AS`; the parent instead samples RSS every 40 ms and kills analysis above 256 MiB. The helper also checks its own peak RSS before analysis and before publishing output, so a short-lived process cannot bypass a lower configured limit between parent samples. These checks reject excessive usage; the watchdog bounds sustained RSS, **not instantaneous peaks**. Failures/timeouts/limits retain syntax evidence with generic diagnostics, not source excerpts. Tests execute the real isolated interpreter when present and exercise no-project-execution sentinels, scope/import ambiguity, Unicode offsets, stale snapshots, private re-export paths, and lower node/output/RSS limits. Deterministic delayed-spawn/completion regressions verify deadlines without assuming a minimum interpreter startup time.

### Snapshot-only Go bindings

A trusted Go 1.20+ installation in a fixed system/Homebrew location can compile the fixed packaged helper into an owned temporary directory. Discovery ignores `PATH`; compilation has a 60-second deadline and disables module mode, workspace discovery, user Go configuration, cgo, toolchain switching and network module proxies. **Only the packaged helper is compiled, never repository source.** Its standard-library `go/parser`, `go/ast` and `go/types` consume bounded snapshot JSON. No project `go build`, `go test`, `go generate`, `go list`, source import execution, repository hook or dependency download runs. The helper uses a snapshot-only importer, not installed package export data. Helper/compiler identity participates in snapshot identity; discovery is cached until service restart. Missing/unsupported compilers retain explicit syntax-only coverage without installation. Fixed discovery paths exist for macOS, Linux and native Windows, but the local proof below is Linux/ARM64; no macOS or Windows native success is inferred from it.

Resolved edges use `resolution.engine: "go-types"` and the actual compiler version. The first supported subset is direct same-package functions across files and direct functions imported from in-snapshot modules, including explicit import aliases and the actual declared package name. Inert `go.mod` supports literal module/go/require declarations; literal `go.work` use entries can connect represented modules. These parsers do not implement full Go module selection. Internal-package boundaries, duplicate import paths, missing/older workspace versions and newer required toolchains are checked conservatively. Every resolved edge retains all source/config references from its package and transitive imported packages, including intermediate private files. Cloud traversal filters that full provenance; current exclusions invalidate historic evidence. Multi-file package imports have no arbitrary single-file graph target, although direct function calls into those packages may resolve.

Unsupported metadata (`replace`, `exclude`, `toolchain`, and other directives), standard-library/external imports, cgo/unsafe/dot/side-effect imports, cyclic or type-invalid packages, build constraints (including a leading UTF-8 BOM), and platform-specific source retain syntax-only evidence. The host independently validates transitive import provenance using full syntax spans; escaped or comment-interleaved import specs that it cannot verify also fail closed. Test/ignored/vendor files are not compiler-resolved. Methods, function values, generics and dynamic call forms are not resolved. This is a declaration-binding layer over the represented snapshot, not a whole-repository build check or complete call graph. See the official [Go module reference](https://go.dev/ref/mod) and [type-checker API](https://pkg.go.dev/go/types#Config) for the underlying language/module distinction.

Bounds are 500 Go files plus 100 metadata files, 250 packages, 4 MiB source, 16 MiB serialized input, 100,000 AST nodes, 5,000 updates, 64 provenance files per edge, 2 MiB combined process output and a 5-second analysis deadline. Go's 192-MiB runtime memory target is **soft**; Unix additionally samples RSS every 40 ms and stops above 256 MiB, which does not bound instantaneous peaks. Native Windows currently has the soft memory target plus wall/output/node limits, not a hard RSS limit. Errors return generic diagnostics with syntax evidence retained.

`GRAPH_ENGINE_GO_DOCKER_TESTS=1 npm test -w @graph-engineering/engine -- tests/context-go.test.ts` compiles/runs the packaged helper in a preprovisioned, network-disabled Go image with a read-only filesystem, dropped capabilities, non-root user and a 256-MiB container memory limit. The observed proof used `go1.24.13 linux/arm64`, image `golang@sha256:d2d2bc1c84f7e60d7d2438a3836ae7d0c847f4888464e7ec9ba3a1339a1ee804`. The Mac has no trusted Go compiler; native cases explicitly skip there. A separate `tests/fixtures/go-runtime/Dockerfile` combines that compiler with the already provisioned verification image to exercise native discovery, helper compilation, analyzer transport and indexed privacy rules inside Linux; no host Go installation is necessary:

```sh
docker build --pull=false --network=none -f packages/engine/tests/fixtures/go-runtime/Dockerfile -t graph-go-native-test:local .
docker run --rm --network=none --read-only --tmpfs /tmp:rw,exec,nosuid,size=768m --memory=1536m --cpus=2 --pids-limit=128 --cap-drop=ALL --security-opt=no-new-privileges graph-go-native-test:local
```

Observed results on 2026-09-22: the Mac-driven helper suite passed 12 cases with 3 native cases skipped; the Linux native fixture passed 9 cases with 6 Docker-orchestration cases skipped. Together these exercise both transports; skipped cases are not counted as successes. The native fixture's dependency base was `graph-engineering-verify:local` image ID `sha256:b8fbb70d27b3989e2243e466c967214cc4496197d8a7a961d8e3907200218a1a`; the packaged helper source SHA-256 was `dc0986a7486397da2908aa7172f0d26440652aa8d8375d77f6a8c01b39d9a66f`. Rebuilding that local base requires re-running the proof, not assuming identical dependencies.

Static binding limits are 1,000 TS/JS files, 8 MiB source, 250,000 AST nodes, a 5-second worker deadline, and a 256-MiB old-generation heap. Limit/error fallback keeps syntax/heuristic evidence with an explicit coverage diagnostic. An unchanged fallback snapshot retains that diagnostic. Parser/compiler/resolver versions participate in snapshot identity; cross-file bindings are recomputed when source or indexed configuration changes rather than reused from per-file parse caches. Historical graph traversal also applies the current exclusion policy to edge targets. These limits and provenance fields are separate from optional semantic-vector retrieval; `index({semantic: false})` still performs bounded compiler analysis. See [platform context coverage](platform.md#context-and-memory) for the same operational boundaries. There is no complete call graph, repository-configured type-check, generated-code discovery, or whole-program dispatch proof.

`getContext({..., retrieval})` accepts `lexical`, `graph`, or `hybrid` (default). Lexical skips model inference and graph expansion; graph uses lexical seeds and bounded neighbors; hybrid adds semantic search when provisioned and lazily repairs missing snapshot vectors. `index({semantic: false})` explicitly performs indexing without loading/invoking embeddings; `index()` preserves semantic indexing by default. Full current-file hashing remains mandatory in both modes. The first hybrid request after a lexical-only index can be slow while uncached vectors are computed. All modes preserve mandatory requirements/constraints and enforce the byte-based conservative token budget.

## Watch, migrations, retention, and backups

`watch({intervalMs = 5000, onIndex?, onError?})` returns an async `close()` handle. Intervals must be 1 second–1 hour. One full content scan runs at a time; the interval starts after completion, so slow scans do not queue. Hash reconciliation detects edits even with unchanged mtimes. Parsed files, embeddings, unchanged snapshots, and deterministic summaries are reused. There is no OS-specific watcher or mtime-only fast path. Explicit `index()` and watcher scans coalesce. A snapshot permits at most 100,000 candidate files and 256 MiB scanned source bytes; individual files larger than 1 MiB are skipped with a diagnostic. Failures do not publish a partial snapshot.

Context schema migrations run in one SQLite `IMMEDIATE` transaction and use `context_metadata.schemaVersion`; the separate operational store owns its own version. Legacy schema is upgraded automatically; newer unknown versions are rejected. This is forward migration support, not arbitrary rollback.

`pruneSnapshots({keepLatest = 20, dryRun = true, protectedSnapshotIds = []})` previews by default. Actual deletion requires `dryRun: false`. It preserves current context, the latest snapshot for every worktree, the requested newest snapshots, every memory's source evidence (including proposed/superseded records), and explicitly pinned IDs. Pin checks and deletes share one write transaction. Unreferenced vectors are pruned too. SQLite may retain freed pages for reuse; no automatic `VACUUM` or deletion of model downloads occurs. **The owner of the separate run store must pass every referenced plan/run snapshot ID under its maintenance lock.** Historical cache entries/review flags for deleted snapshots are removed.

`backup(destination)` uses SQLite's online backup API, preserving committed WAL data, and creates a SHA256 receipt next to a new, private backup file. It refuses an existing destination or a path inside the live data directory. `ContextEngine.restoreBackup({backupPath, dataDir, projectId})` verifies checksum, schema, project identity, and SQLite integrity, then copies into a **new** data directory; it never overwrites live state. Backups are private plaintext, not encrypted. Back up to an access-controlled location. A backup contains **context.sqlite only**: runs.sqlite, downloaded models, repository files, shared knowledge files, configuration, and workspaces require the operational backup coordinator. An interrupted/failed backup can leave an incomplete file without a valid receipt; restore will reject it. Close/reopen application services when switching to a restored directory.

Jina's pinned model assets are SHA256-verified against the provisioning manifest before offline runtime load. The manifest protects against asset corruption; it is not a signature against an attacker who can replace both assets and manifest. Serving never downloads missing models.

### Whole-project operational archive

`backupProject({context, store, dataDir, projectId, destination, config?})` in `operations.ts` composes online backups of both SQLite stores. It copies only the validated `providers.json`, `decisions.json`, and `promotions.json` private configuration files, plus an optional `project-config.reference.json`. Environment variable **names** such as `apiKeyEnv` are preserved; raw credential fields, credential patterns, and endpoint URLs with credentials/query parameters are rejected. No environment file is read. The source directory is never copied recursively.

The project should be idle during backup. Active runs are rejected, SQLite integrity/project/schema identities are checked, and every saved plan must reference a snapshot present in the archive. A concurrent run-store commit or provider-config change aborts completion; a valid manifest is written only at the end. This is a quiescent, checked archive, not a distributed transaction across continuously changing stores.

`restoreProject({backupDirectory, dataDir, projectId})` verifies the strict manifest and every file checksum before restoring into a new directory. A reference project configuration is not automatically installed into a repository. Model weights, working repositories, worker workspaces, external assets, and environment credentials are excluded. Existing unknown/reserved inference costs remain unknown. Restored failed/interrupted runs require manual reconciliation; this archive does **not** claim immediate resumability. Backups are private plaintext; checksums detect corruption but are not signatures against a malicious archive author.

## Repeatable scale benchmark

Build contracts and engine, then run:

```sh
node packages/engine/benchmarks/context.mjs 5000
```

The harness supports 100–10,000 generated TypeScript files, indexes cold/unchanged/one-file-edited states, checks snapshot invariants, measures retrieval and summary latency, and emits a versioned JSON receipt with implementation/harness hashes, hardware, RSS, SQLite/WAL sizes, and exact dataset size. It generates and removes only its own temporary fixture. It does not contact providers or provision embeddings. These are measured synthetic indexing results, not claimed coding accuracy, real-project throughput, semantic quality, or token/cost savings.

An observed 10,000-file/20,000-function run is recorded in [context-benchmark-10000.json](context-benchmark-10000.json): 8.00 s cold, 2.61 s unchanged, 7.83 s after one edit; about 1.51 GiB peak process RSS on Apple M5 Max. Lexical lookup took 2.64 ms and graph-expanded lookup 66.27 ms. This receipt predates compiler-backed static binding and records its own implementation hashes; it is not a benchmark of the newer compiler pass. A 10,000-file TS snapshot exceeds that pass's 1,000-file limit and retains syntax evidence with a coverage diagnostic. Other local inference services were present, so this is not a fully isolated performance comparison. Snapshots currently duplicate code payloads; retaining two snapshots used about 365 MB database plus 187 MB live WAL for 3.15 MB of generated source. Retention is therefore operationally important; these figures are not a claim of storage efficiency.
